# Development Environment Configuration
# This is used for local development and testing with sample data

# Database
DB_NAME=devel.db

# Initial Admin User (created on first migration)
INITIAL_ADMIN_EMAIL=admin@ama-impact.com
INITIAL_ADMIN_PASSWORD=Admin123!
INITIAL_ADMIN_NAME=System Administrator

# Security - Development keys (NOT for production!)
SECRET_KEY=dev-secret-key-change-in-production-ff54559842b522298b159cbf1029da085dd200911e44d685717ffe20c2673201
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480
REFRESH_TOKEN_EXPIRE_DAYS=30

# Email Configuration (use Mailhog or similar for testing)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=dev@ama-impact.com
SMTP_PASSWORD=dev-password
EMAIL_FROM=dev-noreply@ama-impact.com

# CORS - Allow local frontend
FRONTEND_URL=http://localhost:3000

# Application
APP_NAME="AMA IMPACT Development"
APP_VERSION="1.0.0-dev"
DEBUG=True

# Scheduler (run less frequently in dev)
NOTIFICATION_CHECK_HOUR=9
NOTIFICATION_CHECK_MINUTE=0

# Development-specific
ENABLE_SQL_LOGGING=True
AUTO_RELOAD=True
